---
import HeaderLink from "./HeaderLink.astro";
import { SITE_TITLE } from "../consts";
---

<header>
  <nav>
    <HeaderLink href="/">FR</HeaderLink>
    <HeaderLink href="/en">EN</HeaderLink>
  </nav>
</header>

<style>
  header {
    padding-block: var(--space-s);
  }

  nav {
    display: flex;
    align-items: end;
    justify-content: end;
    gap: var(--space-s);
    max-width: 720px;
  }

  a {
    display: inline-block;
    text-decoration: none;
  }

  a:hover {
    font-weight: 800;
    transition: font-weight 0.2s ease-in-out;
  }

  a.active {
    font-weight: 800;
    text-decoration: underline;
    text-decoration-color: var(--gray-11);
    text-underline-offset: max(0.2em, 2.5px);
  }
</style>
